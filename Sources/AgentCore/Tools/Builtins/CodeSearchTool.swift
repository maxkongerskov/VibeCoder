//
//  CodeSearchTool.swift
//
//  CodeSearchTool: not ported — see GrepCodeTool which already provides
//  this functionality. The DEV PLAN's CodeSearchTool bundled `grep_code`
//  + `glob_files` (regex content search + glob file discovery). Claude
//  Edition's AgentCore already ships both as standalone builtins —
//  GrepCodeTool.swift and GlobFilesTool.swift — under the same tool
//  names. Adding a parallel CodeSearchTool would only create namespace
//  ambiguity and tool-schema bloat. This stub exists solely to make the
//  porting checklist's expected destination resolve to a compilable
//  symbol.
//

import Foundation

enum CodeSearchTool {}
