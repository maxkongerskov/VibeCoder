// FontExtensions.swift
// AgentOS
//
// Product type is SF Pro + SF Mono via Theme.Typography.
// `Font.geist` in Theme.swift still stubs to system fonts for legacy call sites.
// Prefer Theme.Typography.* for new UI — never mix .serif into chat/chrome.

import SwiftUI
import CoreText
