# CLI live TTY artifact (Rigel, 2026-08-21)

PTY via `pty.fork` around `.build/debug/vibecoder`. Unit tests are not a TTY.
Backend: oMLX `:8080` model `mlx-community--Qwen3-0.6B-4bit`. Non-git `--project /tmp/vc-cli-p2-*`.

## Notes
- A start
- A prompt=True
- A exit=0 help=True cont=True altscreen=False
- B F4 start
- B prompt=True snap=live2. Worktree mode requires git. | vibecoder  model=mlx-community--Qwen3-0.6B-4bit  project=/tmp/vc-cli-p2-live2 | Type /help. Ctrl+C cancels the current turn (returns to ›); at the prompt it exits. | 
- B waiting 0s bytes=276 dead=False
- B waiting 10s bytes=276 dead=False
- B waiting 20s bytes=276 dead=False
- B waiting 30s bytes=276 dead=False
- B waiting 40s bytes=276 dead=False
- B waiting 50s bytes=276 dead=False
- B waiting 60s bytes=276 dead=False
- B waiting 70s bytes=276 dead=False
- B after-turn done=True error=False dead=False extra_bytes=61
- B tail=Not a git repository: /tmp/vc-cli-p2-live2. Worktree mode requires git. | vibecoder  model=mlx-community--Qwen3-0.6B-4bit  project=/tmp/vc-cli-p2-live2 | Type /help. Ctrl+C cancels the current turn (returns to ›); at the prompt it exits. | › Reply with only the word hello. |  |  | hello | [done] stop | 
- B back_to_prompt=True dead=False
- B K1 SIGINT sent
- B K1 back_to_prompt=True dead=False tail=Not a git repository: /tmp/vc-cli-p2-live2. Worktree mode requires git. | vibecoder  model=mlx-community--Qwen3-0.6B-4bit  project=/tmp/vc-cli-p2-live2 | Type /help. Ctrl+C cancels the current turn (returns to ›); at the prompt it exits. | › Reply with only the word hello. |  |  | hello | [done] stop | Write a long numbered list from 1 to 80 with one sentence each. | › 
- B K2 idle SIGINT sent
- B K2 exit=killed (SIGINT during post-cancel `[done] cancelled` race — handler still armed)
- B csi=False bytes=385 f4=True
- Dedicated idle K2 (fresh process, at `›`, no in-flight turn): PTY `^C` + `SIGINT` → **exited signal 2** immediately. Tail showed `› ^C`.
- R2 persist: `~/Library/Application Support/VibeCoder/conversations/70256FC5-….json` title CLI, user+assistant hello, cancelled list user line, `worktreeBranch` nil.
- R5 git (earlier session): conversation `5350FC21` `worktreeBranch=agentcore/5350fc21`. Sibling cleaned up.
- C2 NO_COLOR TTY `/exit`: no CSI. Pipe `--help`: no CSI.

## Transcript `A-r1`

```
Not a git repository: /tmp/vc-cli-p2-live2. Worktree mode requires git.
vibecoder  model=mlx-community--Qwen3-0.6B-4bit  project=/tmp/vc-cli-p2-live2
Type /help. Ctrl+C cancels the current turn (returns to ›); at the prompt it exits.
› /help
usage: vibecoder [chat] [--project PATH] [--backend NAME] [--model ID] [--resume UUID]
backends: lmstudio | omlx | ollama | unsloth | exo | custom
commands: /help /exit /quit /new
Ctrl+C cancels the current turn (returns to ›); at the prompt it exits.
› /ex\
… it

```

## Transcript `B-live`

```
Not a git repository: /tmp/vc-cli-p2-live2. Worktree mode requires git.
vibecoder  model=mlx-community--Qwen3-0.6B-4bit  project=/tmp/vc-cli-p2-live2
Type /help. Ctrl+C cancels the current turn (returns to ›); at the prompt it exits.
› Reply with only the word hello.


hello
[done] stop
Write a long numbered list from 1 to 80 with one sentence each.
› 
[done] cancelled
› 
```